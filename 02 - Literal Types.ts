//Literal Types

export type TheColorBlue = 'blue';

//NOTE: the //^? syntax is called a two-slash query.
//They are enabled by the TwoSlash plugin in VS Code or the WITT plugin in IntelliJ

export type Zero = 0;

const notBlue: TheColorBlue = 'red';
const blue: TheColorBlue = 'blue';

const zero: Zero = 1;
